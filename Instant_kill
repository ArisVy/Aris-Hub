local Players = game:GetService("Players") -- Đã sửa lỗi viết hoa chữ 'Local'
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer
local onenabledshotho = false

-- Chèn vào dòng 7:
local function ThongBao(noiDung)
    local TweenService = game:GetService("TweenService")
    local PlayerGui = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")
    
    if PlayerGui:FindFirstChild("ArisNotify") then
        PlayerGui.ArisNotify:Destroy()
    end

    local gui = Instance.new("ScreenGui")
    gui.Name = "ArisNotify"
    gui.Parent = PlayerGui
    gui.ResetOnSpawn = false

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 280, 0, 70)
    frame.Position = UDim2.new(1, 20, 0.7, 0) 
    frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    frame.BackgroundTransparency = 0.2
    frame.BorderSizePixel = 0
    frame.Parent = gui

    local frameCorner = Instance.new("UICorner")
    frameCorner.CornerRadius = UDim.new(0, 8)
    frameCorner.Parent = frame

    local icon = Instance.new("ImageLabel")
    icon.Size = UDim2.new(0, 50, 0, 50)
    icon.Position = UDim2.new(0, 10, 0.5, -25)
    icon.BackgroundTransparency = 1
    icon.Image = "rbxassetid://125329301331069"
    icon.Parent = frame

    local iconCorner = Instance.new("UICorner")
    iconCorner.CornerRadius = UDim.new(0, 25)
    iconCorner.Parent = icon

    local title = Instance.new("TextLabel")
    title.Text = "Thông báo"
    title.Size = UDim2.new(1, -80, 0, 20)
    title.Position = UDim2.new(0, 70, 0.2, 0)
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.Font = Enum.Font.SourceSansBold
    title.TextSize = 16
    title.BackgroundTransparency = 1
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = frame

    local desc = Instance.new("TextLabel")
    desc.Text = noiDung or "Discord: Aris_vy\nFacebook: Trọng Vỹ'z"
    desc.Size = UDim2.new(1, -80, 0, 30)
    desc.Position = UDim2.new(0, 70, 0.5, 0)
    desc.TextColor3 = Color3.fromRGB(220, 220, 220)
    desc.Font = Enum.Font.SourceSans
    desc.TextSize = 14
    desc.BackgroundTransparency = 1
    desc.TextXAlignment = Enum.TextXAlignment.Left
    desc.Parent = frame

    local targetPos = UDim2.new(1, -300, 0.7, 0)
    TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Position = targetPos}):Play()

    task.delay(5, function()
        if frame then
            local tweenOut = TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {Position = UDim2.new(1, 20, 0.7, 0)})
            tweenOut:Play()
            tweenOut.Completed:Connect(function() gui:Destroy() end)
        end
    end)
end

-- Sau khi chèn xong hàm trên, bạn chỉ cần gọi lệnh này để hiện thông báo:
ThongBao("Đã tải script Aris Hub thành công!")

-- GUI
local screenGui = Instance.new("ScreenGui")
screenGui.ResetOnSpawn = false
screenGui.Parent = game.CoreGui

-- Khung chính (Viên thuốc)
local buttonFrame = Instance.new("Frame")
buttonFrame.Size = UDim2.new(0, 160, 0, 60)
buttonFrame.Position = UDim2.new(0.1, 0, 0.7, 0)
buttonFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
buttonFrame.BorderSizePixel = 0
buttonFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1, 0) -- Bo tròn tối đa thành viên thuốc
corner.Parent = buttonFrame

-- 🔹 Nền Cầu Vồng (Chỉ hiện khi bật)
local rainbowBG = Instance.new("Frame")
rainbowBG.Size = UDim2.new(1, 0, 1, 0)
rainbowBG.BackgroundTransparency = 0
rainbowBG.Visible = false
rainbowBG.Parent = buttonFrame

local bgCorner = Instance.new("UICorner")
bgCorner.CornerRadius = UDim.new(1, 0)
bgCorner.Parent = rainbowBG

local bgGradient = Instance.new("UIGradient")
bgGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromHSV(0, 1, 1)),
    ColorSequenceKeypoint.new(0.5, Color3.fromHSV(0.5, 1, 1)),
    ColorSequenceKeypoint.new(1, Color3.fromHSV(1, 1, 1))
})
bgGradient.Parent = rainbowBG

-- 🔹 Chữ Aris Hub (Nằm giữa)
local title = Instance.new("TextLabel")
title.Size = UDim2.new(0.8, 0, 0.6, 0)
title.Position = UDim2.new(0.5, 0, 0.5, 0)
title.AnchorPoint = Vector2.new(0.5, 0.5) -- Đưa tâm chữ vào giữa khung
title.BackgroundTransparency = 1
title.Text = "Aris Hub"
title.Font = Enum.Font.GothamBold
title.TextScaled = true
title.ZIndex = 5
title.Parent = buttonFrame

-- 🔹 Hiệu ứng cầu vồng cho chữ (Luôn chạy)
local txtGradient = Instance.new("UIGradient")
txtGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromHSV(0, 1, 1)),
    ColorSequenceKeypoint.new(0.5, Color3.fromHSV(0.5, 1, 1)),
    ColorSequenceKeypoint.new(1, Color3.fromHSV(1, 1, 1))
})
txtGradient.Parent = title

local titleStroke = Instance.new("UIStroke")
titleStroke.Thickness = 2
titleStroke.Color = Color3.new(0, 0, 0) -- Viền đen cố định
titleStroke.Parent = title

-- 🔹 Nút bấm
local button = Instance.new("TextButton")
button.Size = UDim2.new(1, 0, 1, 0)
button.BackgroundTransparency = 1
button.Text = ""
button.ZIndex = 10
button.Parent = buttonFrame

-- 🔹 Vòng lặp tạo hiệu ứng cầu vồng chuyển động
RunService.RenderStepped:Connect(function()
    local rot = (tick() * 100) % 360 -- Tốc độ quay màu
    txtGradient.Rotation = rot
    bgGradient.Rotation = rot
end)

-- Logic Toggle
button.MouseButton1Click:Connect(function()
    local char = LocalPlayer.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not hrp or not humanoid then return end

    -- Kiểm tra nếu chưa bật mà máu đã tụt xuống 20% hoặc thấp hơn thì từ chối bật
    if not onenabledshotho and humanoid.Health <= (humanoid.MaxHealth * 0.2) then
        return
    end

    onenabledshotho = not onenabledshotho
    
    -- Đổi trạng thái nền
    rainbowBG.Visible = onenabledshotho
    buttonFrame.BackgroundColor3 = onenabledshotho and Color3.new(1,1,1) or Color3.new(0,0,0)

    if onenabledshotho then
        task.spawn(function()
            while onenabledshotho do
                local currentHumanoid = char:FindFirstChildOfClass("Humanoid")
                
                -- Liên tục kiểm tra, nếu máu dưới hoặc bằng 20% thì tự động dừng
                if not currentHumanoid or currentHumanoid.Health <= (currentHumanoid.MaxHealth * 0.2) then
                    onenabledshotho = false
                    rainbowBG.Visible = false
                    buttonFrame.BackgroundColor3 = Color3.new(0,0,0)
                    break
                end
                
                local pos = hrp.Position
                hrp.CFrame = CFrame.new(pos.X, pos.Y - 999999999, pos.Z) -- Giữ nguyên logic hạ thấp tọa độ của bạn
                task.wait(0.01)
            end
        end)
    end
end)

-- Hệ thống kéo thả (Drag)
local dragging, dragInput, dragStart, startPos
button.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = buttonFrame.Position
    end
end)
button.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        buttonFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
loadstring(game:HttpGet("https://raw.githubusercontent.com/ArisVy/Aris-Hub/refs/heads/main/Notification"))()
local TweenService = game:GetService("TweenService")
local PlayerGui = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")

-- Xóa thông báo cũ nếu đang chạy lại script
if PlayerGui:FindFirstChild("RobloxCustomNotify") then
    PlayerGui.RobloxCustomNotify:Destroy()
end

local gui = Instance.new("ScreenGui")
gui.Name = "RobloxCustomNotify"
gui.Parent = PlayerGui
gui.ResetOnSpawn = false

-- Khung nền (Giống Notification mặc định)
local frame = Instance.new("Frame")
frame.Name = "NotifyFrame"
frame.Size = UDim2.new(0, 280, 0, 70)
-- Position: 0.7 ở đây là độ cao (càng nhỏ càng cao lên trên)
frame.Position = UDim2.new(1, 20, 0.7, 0) 
frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
frame.BackgroundTransparency = 0.2 -- Độ trong suốt nhẹ
frame.BorderSizePixel = 0
frame.Parent = gui

local frameCorner = Instance.new("UICorner")
frameCorner.CornerRadius = UDim.new(0, 8)
frameCorner.Parent = frame

-- Ảnh đại diện (Bo cong 25px như yêu cầu)
local icon = Instance.new("ImageLabel")
icon.Name = "Icon"
icon.Size = UDim2.new(0, 50, 0, 50)
icon.Position = UDim2.new(0, 10, 0.5, -25) -- Giữa trái
icon.BackgroundTransparency = 1
icon.Image = "rbxassetid://125329301331069"
icon.Parent = frame

local iconCorner = Instance.new("UICorner")
iconCorner.CornerRadius = UDim.new(0, 25) -- Bo tròn 25px
iconCorner.Parent = icon

-- Tiêu đề (Thông báo)
local title = Instance.new("TextLabel")
title.Name = "Title"
title.Text = "Thông báo"
title.Size = UDim2.new(1, -80, 0, 20)
title.Position = UDim2.new(0, 70, 0.2, 0)
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.SourceSansBold
title.TextSize = 16
title.BackgroundTransparency = 1
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame

-- Nội dung (Thông tin liên hệ)
local desc = Instance.new("TextLabel")
desc.Name = "Description"
desc.Text = "Discord: Aris_vy\nFacebook: Trọng Vỹ'z"
desc.Size = UDim2.new(1, -80, 0, 30)
desc.Position = UDim2.new(0, 70, 0.5, 0)
desc.TextColor3 = Color3.fromRGB(220, 220, 220)
desc.Font = Enum.Font.SourceSans
desc.TextSize = 14
desc.BackgroundTransparency = 1
desc.TextXAlignment = Enum.TextXAlignment.Left
desc.Parent = frame

-- Hiệu ứng trượt vào (Slide In)
local targetPos = UDim2.new(1, -300, 0.7, 0)
local tweenIn = TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Position = targetPos})
tweenIn:Play()

-- Tự động biến mất sau 5 giây
task.delay(5, function()
    local tweenOut = TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {Position = UDim2.new(1, 20, 0.7, 0)})
    tweenOut:Play()
    tweenOut.Completed:Connect(function()
        gui:Destroy()
    end)
end)
